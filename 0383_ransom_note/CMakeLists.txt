# this is the build file for project 
# it is autogenerated by the xmake build system.
# do not edit by hand.

# project
cmake_minimum_required(VERSION 3.15.0)
cmake_policy(SET CMP0091 NEW)
project(0383_ransom_note LANGUAGES CXX C)

# target
add_executable(0383_ransom_note "")
set_target_properties(0383_ransom_note PROPERTIES OUTPUT_NAME "0383_ransom_note")
set_target_properties(0383_ransom_note PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_SOURCE_DIR}/build/macosx/x86_64/release")
target_compile_options(0383_ransom_note PRIVATE
    $<$<COMPILE_LANGUAGE:C>:-target>
    $<$<COMPILE_LANGUAGE:CXX>:-target>
    $<$<COMPILE_LANGUAGE:C>:x86_64-apple-macos12.6>
    $<$<COMPILE_LANGUAGE:CXX>:x86_64-apple-macos12.6>
    $<$<COMPILE_LANGUAGE:C>:-isysroot>
    $<$<COMPILE_LANGUAGE:CXX>:-isysroot>
    $<$<COMPILE_LANGUAGE:C>:/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX13.0.sdk>
    $<$<COMPILE_LANGUAGE:CXX>:/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX13.0.sdk>
    $<$<COMPILE_LANGUAGE:C>:-DNDEBUG>
    $<$<COMPILE_LANGUAGE:CXX>:-DNDEBUG>
)
if(MSVC)
    target_compile_options(0383_ransom_note PRIVATE $<$<CONFIG:Release>:-Ox -fp:fast>)
else()
    target_compile_options(0383_ransom_note PRIVATE -O3)
endif()
if(MSVC)
else()
    target_compile_options(0383_ransom_note PRIVATE -fvisibility=hidden)
endif()
if(MSVC)
    set_property(TARGET 0383_ransom_note PROPERTY
        MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
endif()
target_link_libraries(0383_ransom_note PRIVATE
    z
)
target_link_options(0383_ransom_note PRIVATE
    -stdlib=libc++
)
target_sources(0383_ransom_note PRIVATE
    src/main.cpp
)

